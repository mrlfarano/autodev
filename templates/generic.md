## Framework: Generic

### Agent Context
- No framework-specific knowledge available
- Read the project's README and build config for context
- Identify the test command, build command, and lint command from project config
- Adapt your approach based on what you find

### Scoring Overrides

# Generic template intentionally defines no hard_gate or metrics commands.
# The user MUST define these in autodev.yaml for their specific project.
# autodev-score will error if no hard_gate commands are configured.
